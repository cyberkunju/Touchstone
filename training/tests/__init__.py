"""Test package for the training tools (synthgen, etc.).

Made a real package so `python -m tests.test_synthgen` resolves the local
suite (a regular package on the cwd shadows any same-named site-packages dir).
"""
