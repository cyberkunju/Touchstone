# Synthetic document generator + detector training (Phase 0/1).
# Generation deps are light (CPU). Training deps (ultralytics/torch) are installed
# separately on the GPU box to match its CUDA version.

# --- generation ---
Pillow>=10.2.0
numpy>=1.26.0
opencv-python>=4.9.0
qrcode>=7.4.2
python-barcode>=0.15.1
PyYAML>=6.0.1
faker>=24.0.0

# --- ontology / benchmark instruments (hard import-time deps) ---
# shapely: polygon/quad IoU + ignore-region masking in benchmarks/metrics_v2.py
# imagehash: pinned perceptual hash for leakage-dedup in ontology/provenance.py
shapely>=2.0.0
imagehash>=4.3.0

# --- training / export (install on GPU box; see training/README.md) ---
# ultralytics>=8.3.0
# torch  (install the CUDA build matching the box)
# onnx>=1.16.0
# onnxruntime>=1.18.0
