# Perception service — pinned majors, Python 3.11+
# Core (P3.2-P3.4 stage/bundle tests run on these alone):
onnxruntime>=1.22,<2
numpy>=2,<3
pillow>=11,<13
pypdfium2>=4,<5
openpyxl>=3.1,<4
python-docx>=1.1,<2
jsonschema>=4.23,<5
opencv-python-headless>=4.10
zxing-cpp>=2.2

# Skeleton (P3.1):
fastapi>=0.115,<1
uvicorn>=0.30,<1
python-multipart>=0.0.9,<1

# Dev:
pytest>=8,<10
# FastAPI's allowed range spans Starlette's httpx → httpx2 transition.
# Install both transports so TestClient works across the full supported range.
httpx>=0.27,<0.29
httpx2>=2,<3
# test_packaging builds with --no-build-isolation by design, so the caller
# must provide the PEP 517 backend declared in pyproject.toml.
setuptools>=68
